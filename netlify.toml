[build]
environment = { SECRETS_SCAN_OMIT_KEYS = "VITE_API_KEY,VITE_ANCHOR_DATE" }
  command = "npm run build" 
  publish = "dist"  

functions = "functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200